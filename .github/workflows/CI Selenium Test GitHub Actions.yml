name: CI Selenium Test GitHub Actions

on: [push]
    

jobs:
  SeleniumTestRun:

    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v3
      - name: Build with Maven
        run: mvn clean test
