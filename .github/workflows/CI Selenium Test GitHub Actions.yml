name: CI Selenium Test GitHub Actions

on:
  push:
    branches: [ "OctaneIntegration" ]
  pull_request:
    branches: [ "OctaneIntegration" ]

jobs:
  SeleniumTestRun:

    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v3
      - name: Build with Maven
        run: mvn clean test